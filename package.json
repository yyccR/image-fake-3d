{
  "name": "image-3d-parallax-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Local SHARP and Gaussian-splat spatial wallpaper experiment",
  "repository": "https://github.com/yyccR/image-fake-3d.git",
  "workspaces": [
    "apps/desktop",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup:sharp": "bash scripts/setup-sharp.sh",
    "serve": ".venv/bin/python sharp_server.py",
    "desktop:dev": "npm run tauri dev --workspace @image-fake-3d/desktop",
    "desktop:build": "npm run tauri build --workspace @image-fake-3d/desktop",
    "test": "node --experimental-default-type=module --check app.js && node --experimental-default-type=module --check gaussian-renderer.js && node --experimental-default-type=module --check packages/spatial-renderer/runtime.js && node --experimental-default-type=module --check apps/desktop/src/main.js && python3 -m py_compile sharp_server.py scripts/download-birefnet-model.py && bash -n scripts/setup-sharp.sh && bash -n scripts/download-sharp-model.sh && node --test tests/*.test.js && python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "devDependencies": {
    "@playcanvas/splat-transform": "3.1.7"
  }
}
